import firebase_admin
from firebase_admin import credentials, db

# ফাইলের লোকেশন দিন
cred = credentials.Certificate("serviceAccountKey.json")

# Firebase Initialize
firebase_admin.initialize_app(cred, {
    'databaseURL': 'https://fir-dashboard-e53de.firebaseio.com/'
})
